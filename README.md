# wikitest

Rebuild target for **wiki-brain** — a source-grounded, longitudinal knowledge
graph and cognitive modeling system.

> **This repository is public, deliberately.**

## State: rebuilding

The original working tree was wiped. This repo is being reconstituted from
the surviving copies. **[`RECOVERY.md`](RECOVERY.md)** is the current map of
what survived, where, and how faithfully — every claim in it was tested, not
assumed.

The short version:

- **The message corpus is fully recoverable** — re-verified byte-exact
  against its manifest in this session. No time fidelity is at risk on the
  message record.
- **The engine survived** — `app.py` and all 40 `bin/` tools are byte-exact
  on the Drive staging copy. Only `.md` files were damaged by that copy's
  Google Docs conversion.
- **The wiki body is waiting on a push from the Mac.** That copy is real
  Markdown with real git history and is worth more than any reconstruction.

This README is provisional and will be replaced by the merged original.
